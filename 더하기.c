#include <stdio.h>
int main()
{
	char a[10000][61]={0}, b[51]={0}, b[10]={0}, integer_sum[50]={0}, point_sum[9]={0};
	int abc;
	for{int x=0;x<=i;x++}													// 정수부 계싼
		abc=strlen(a[x])
			for(int y=0;y<=abc-1;y++)
				if(a[x][y]=='+')
					{def1=strlen(b[x-1])-1;
					 def2=strlen(b[x+1])-1;
					for(int hij = 50;def1<0&&def2<0;def1--,def2--,hij--)
						if(def1>=0&&def2>=0)
						sum[hij]+=b[x-1][def1]+b[x+1][def2];
						if(def1<0&&def2>=0)
						sum[hij]+=b[x+1][def2];
						if(def1>=0&&def2<0)
						sum[hij]+=b[x-1][def1];	
						if(sum[hij]>=10)
						sum[hij-1]++;
						sum[hij]=sum[hij]-10;
					


		

